import * as React from 'react'
import { materialize, withDefaultTag } from '../Base'

export default materialize('mdc-top-app-bar__title', {
  displayName: 'TopAppBarTitle',
})(withDefaultTag('span'))
