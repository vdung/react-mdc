import * as React from 'react'
import { materialize, withDefaultTag } from '../Base'

export default materialize('mdc-list-item__graphic', {
  displayName: 'ListItemGraphic',
})(withDefaultTag('span'))
