import * as React from 'react'
import { materialize, withDefaultTag } from '../Base'

export default materialize('mdc-list-item__primary-text', {
  displayName: 'ListItemPrimaryText',
})(withDefaultTag('span'))
